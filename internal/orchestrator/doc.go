// Package orchestrator turns tasks (project+branch instances) into running local dev environments: config resolution, worktree lookup, process launch, proxy routes, registry, and status/logs.
package orchestrator
